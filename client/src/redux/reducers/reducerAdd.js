import {ADD} from '../actions/actiontupes'

const rediserADD = (state={}, action)=>{
  switch (action.type) {
    // Используем переменную в case
    case ADD:
    return state;
    default:
    return state;
}

}
export default rediserADD
